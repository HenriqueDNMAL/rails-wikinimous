<h1>Details of <%= @article.title %></h1>

<p><strong>Content:</strong> <%= @article.content %> </p>

<%= link_to 'Delete this article', article_path(@article), data: { turbo_method: :delete,
                                                                            turbo_confirm: 'Are you sure you want to delete this article?' } %>

<%= link_to 'Back to list', articles_path %>
